/**
 * Admin UI for this plugin is injected into Content Manager list views
 * from src/admin/app.tsx (Template / Export / Import buttons + order totals).
 * No separate plugin page is registered — that route 404'd for local plugins.
 */
export default {
  register() {},
};
