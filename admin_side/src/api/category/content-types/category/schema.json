{
  "kind": "collectionType",
  "collectionName": "categories",
  "info": {
    "singularName": "category",
    "pluralName": "categories",
    "displayName": "Category",
    "description": "Shop sections — T-Shirts, Hoodies, Pants, etc."
  },
  "options": {
    "draftAndPublish": true
  },
  "attributes": {
    "name": {
      "type": "string",
      "required": true,
      "description": "Name customers see, e.g. T-Shirts"
    },
    "link_name": {
      "type": "uid",
      "targetField": "name",
      "required": false,
      "description": "Filled in automatically — used for the website link",
      "pluginOptions": {
        "content-manager": {
          "visible": false
        }
      }
    },
    "photo": {
      "type": "media",
      "multiple": false,
      "required": false,
      "allowedTypes": ["images"],
      "description": "Picture for this category — upload like product photos"
    },
    "list_position": {
      "type": "integer",
      "default": 0,
      "description": "Order on the website (1 = show first, 2 = second, etc.)"
    },
    "products": {
      "type": "relation",
      "relation": "oneToMany",
      "target": "api::product.product",
      "mappedBy": "category",
      "description": "Products that use this category (managed on the Product form)",
      "pluginOptions": {
        "content-manager": {
          "visible": false
        }
      }
    }
  }
}
